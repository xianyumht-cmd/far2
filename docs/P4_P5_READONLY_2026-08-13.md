# FAR2 P4/P5 只读扩展基线 — 2026-08-13

状态：**P4A / P5A / P5B SOURCE + BUILD COMPLETE / LIVE READ ACCEPTANCE PENDING**

## 总原则

这一阶段继续采用“先读后写”边界：

- 有可靠读协议先做只读；
- 真实回包没确认前，不补领取/穿戴/购买/修改等写操作；
- unknown 字段尽量保留，不靠猜测解释；
- 真实读取验收不阻塞后续只读开发。

## P4A — 护主犬状态只读

正式合并：

```text
d05715b4a09132cd1466b9f042e4c266007e18b9
```

正式链：

```text
个人 -> 护主犬
  -> GET /api/dog/info
  -> DataProvider
  -> Worker getDogInfo
  -> DogService.GetDogInfo
```

当前读取：

- 狗列表：id / level / status / active / expireTime；
- coin；
- protectTime；
- 狗粮 id / duration / count；
- raw protobuf 顶层 field 7（如果存在）保留为 `claimableGiftCount`。

自己的 `GetDogInfo` 当前按已观察到的空 protobuf body 请求，不主动补 `host_gid`。

明确未实现：

- `ClaimSkillGifts`；
- 喂食；
- 激活/切换；
- 好友狗探测；
- 自动狗狗操作。

P4B 领取写操作继续锁定，必须先看一次真实 P4A 回包。

## P5A — 个人生涯只读

正式合并：

```text
bab9c1bfd1b7a2a920699458efc71e385b19584a
```

正式链：

```text
个人 -> 个人生涯
  -> GET /api/career/info
  -> DataProvider
  -> Worker getCareerInfo
  -> CareerService.CareerInfoGet
```

当前实现：

- typed protobuf 优先；
- raw protobuf fallback，应对字段漂移；
- raw fallback 保留 UTF-8 中文昵称；
- CareerInfoGet 单次超时 10 秒；
- fruitId 复用当前 gameConfig 映射名称/图片/等级/稀有度；
- UI 显示玩家资料、统计总量、Top 3、收获明细；
- UI 保留 `decodeMode` 和响应字节数，方便首次实机验收。

明确未实现：

- 生涯领奖；
- 修改个人资料；
- 任何自动生涯操作。

## P5B — 头像框库存只读

正式合并：

```text
5a708cee266d9e9feb16b3dc2e07480fb0b981f1
```

当前没有可靠的 `AvatarFrame` 内部 protobuf 定义。成熟协议只留下：

```text
BasicInfo.field11 = equip_avatar_frames
GameFriend.field13 = equip_avatar_frames
```

因此 FAR2 不猜“当前佩戴”结构。

P5B 只复用已存在的 Bag 读取：

```text
个人 -> 头像框
  -> GET /api/appearance/avatar-frames
  -> DataProvider
  -> Worker getAvatarFrames
  -> appearance service
  -> existing getBagDetail()
  -> ItemService.Bag
  -> filter itemType=10 && count>0
```

当前游戏配置中已知头像框物品属于 `ItemInfo.type=10`。页面只显示实际拥有库存，并明确：

```text
当前佩戴：待协议确认
```

明确未实现：

- 解析/猜测 `equip_avatar_frames`；
- 穿戴；
- 使用；
- 购买；
- 资料修改。

## 验证

P4A、P5A、P5B 均经过独立的只读 GitHub Actions 完整验证：

- `pnpm install --frozen-lockfile`；
- 对应纯本地 fixture/selftest；
- `pnpm build:web`（`vue-tsc -b && vite build`）；
- 后端 `node --check`；
- `git diff --check`。

验证 workflow 使用 `contents: read`，运行结束后通过 GitHub API 单独删除，不从安装后的 Actions 工作区提交任何文件。

## 实机验收

本轮三个真实验收都只需要读取，不需要任何写操作：

- `个人 -> 护主犬`：确认真实 GetDogInfo 字段；
- `个人 -> 个人生涯`：确认 typed/raw decode 哪个匹配线上；
- `个人 -> 头像框`：确认背包 type=10 实际拥有列表。

空列表、field 7 未返回、当前没有头像框，都属于合法只读结果。

真实读结果未确认前：

- 不进入 P4B ClaimSkillGifts；
- 不实现头像框穿戴/购买；
- 不以“成熟版本可能有”为理由猜写协议。
